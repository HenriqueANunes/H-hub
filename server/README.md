# server — backend do H-hub

API em **Go** + **PostgreSQL** (instância compartilhada do homelab, banco `my_platform`).

> A implementar. Ver roadmap no vault (nota "App Flutter - versão web"):
> começar pelo "esqueleto que anda" — `GET /contas` ponta a ponta
> (Flutter → Go → Postgres → JSON) — e só depois expandir pro CRUD.
